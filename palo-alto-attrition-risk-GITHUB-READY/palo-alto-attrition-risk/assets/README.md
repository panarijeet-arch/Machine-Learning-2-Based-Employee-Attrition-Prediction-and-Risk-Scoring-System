# assets/

Drop a legitimately-licensed Palo Alto Networks logo file here, named
`logo.png`, and the app will automatically use it in the header instead
of the text-based wordmark (see `src/theme.py` -> `brand_header()`).

No logo file is included in this repo -- the actual company logo is a
copyrighted trademark asset that isn't reproduced here.
